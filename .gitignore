# --- secrets: NEVER commit the operator's private key ---
.env
.env.*
!.env.example

# C++ build output
build/
build-*/
*.o
*.a
compile_commands.json
.cache/

# python venv / bytecode (repo is C++-only; keep these so a stray .venv or .pyc
# can never be accidentally committed again)
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/

# runtime state (the SQLite ledger, pool cache, logs, kill flag)
*.db
state/
*.log
safe_pools.json
KILL

# editor / OS
.DS_Store
.coverage
.coverage.*
