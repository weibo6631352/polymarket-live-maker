# --- secrets: NEVER commit the operator's private key ---
.env
.env.*
!.env.example

# python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# tooling
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.mypy_cache/
.ruff_cache/

# runtime state (the engine's SQLite ledger, pool cache, logs, kill flag)
*.db
state/
*.log
safe_pools.json
KILL
