# Archived evidence + harness (from the former polymarket-edge-lab)

Raw experiment data behind the findings in `../`. The Python measurement scripts
that produced this evidence have been removed (the project is now C++-only); this
directory is kept as **historical evidence only**. The live implementation lives in
`cpp/src/pmm/` (rewards, maker_sim, maker_live, portfolio, orderbook). To recover the
old scripts: `git checkout python-final`.
